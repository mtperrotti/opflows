{
  "name": "jira-webhook-server",
  "version": "1.0.0",
  "description": "A simple web server that sends webhooks to Jira",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-deps": "npm install",
    "setup": "npm install && echo 'Setup complete! Run npm start to start the server.'",
    "debian-setup": "sudo apt-get update && sudo apt-get install -y nodejs npm && npm install"
  },
  "dependencies": {
    "express": "^4.22.1",
    "axios": "^1.12.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": ["jira", "webhook", "server", "express"],
  "author": "",
  "license": "MIT"
}
